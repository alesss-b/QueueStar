# Queuesmart

About Queuesmart...

## Installation
1. Install WSL
2. Install Docker Engine
3. Clone repository
4. Run `$ docker build -t queuesmart .` in project root directory
5. `$ docker compose up`
